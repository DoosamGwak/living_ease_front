import classNames from "classnames/bind";
import styles from "./NotYetPage.module.css";

const cx = classNames.bind(styles);

const NotYetPage = () => {
  return <div className={cx("pet-family", "img1")}></div>;
};

export default NotYetPage;
